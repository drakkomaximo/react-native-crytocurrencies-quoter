/* eslint-disable prettier/prettier */
import React, {FC} from 'react';
import { Text } from 'react-native';
import styles from './style';

const Header: FC = () => {
  return <Text style={styles.header}>Crytocurrencies</Text>;
};

export default Header;
